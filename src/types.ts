export enum ScreenSize {
  MOBILE = 'mobile',
  TABLET = 'tablet',
  SMALL_DESKTOP = 'small-desktop',
  DESKTOP = 'desktop',
}

export enum SvgState {
  IDLE = 'idle',
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum ExportType {
  NAMED = 'named',
  DEFAULT = 'default',
}

